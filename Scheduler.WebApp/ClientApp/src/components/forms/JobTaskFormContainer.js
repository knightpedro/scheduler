import React from "react";
import JobTaskForm from "./JobTaskForm";

const JobTaskFormContainer = ({ values }) => {
  const handleCancel = () => {};
  const handleSubmit = (values, { setSubmitting }) => {};

  return <JobTaskForm values={values} />;
};
